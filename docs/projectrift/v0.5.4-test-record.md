# ProjectRift v0.5.4 test record

Date: 2026-07-13  
Branch: `main`  
Base commit: `a53febd`

## Implemented scope

- Registered `UPRShipRepairDataAsset` as `ProjectRiftShipRepair` and created `/Game/ProjectRift/ShipRepairs/DA_RepairEngineStage1` for `Repair.Ship.Engine.Stage1`.
- Added strict contract/catalog validation, story/resource/module availability policy, exact resource deduction, monotonic module upgrades, and monotonic Chapter One unlock.
- Upgraded player saves to v4 with a 128-entry repair transaction ledger and chained v1-to-v2-to-v3-to-v4 migration.
- Added client-owned transactional repair receipts with expected-absolute-state validation, cross-restart idempotency, write-failure retention, five-second lobby retry, and server acknowledgement.
- Extended multiplayer profile projections and seamless PlayerState travel with personal ship modules and pending repair persistence state.
- Added a native Development/Shipping repair panel opened with R in `L_ShipLobby`, closed with Esc, centered independently from diagnostic HUDs, and mutually exclusive with Inventory.
- Added a non-Shipping acceptance helper that only supplies the required story node and raises `EnergyCrystal` to the minimum cost.

No cyclic damage, team resource donations, crafting, Chapter One mission content, formal chapter selection, Steam Cloud, anti-cheat, or scene repair terminal was implemented.

## TDD evidence

- Profile contract tests first failed on schema v3, the missing repair ledger, missing repair DataAsset, and missing receipt type.
- Repair contract/policy tests first failed on missing reflected fields and then on invalid catalog, prerequisite, resource, module, chapter, and receipt behavior.
- v4 ledger tests first failed on migration defaulting, deduplication, invalid GUID removal, oldest-first trimming, and the 128-entry cap.
- Persistence tests first failed on the unimplemented local transaction, duplicate handling, tamper rejection, and write-failure retry; all now pass.
- Multiplayer tests first failed on missing module projection/RPC contracts and then on travel preservation, ready blocking, and authoritative PlayerState application; all now pass.
- UI tests first failed on the missing native widget and controller lifecycle API. A later real PIE check exposed Escape being consumed by the editor, so a keyboard-focus regression test was added red-first; the focusable widget fix now passes.

## Automated verification

### Builds

- `ProjectAEditor Win64 Development`: succeeded; latest log `Saved/Logs/v0.5.4-escape-green-build.out.log`.
- `ProjectA Win64 Development`: succeeded; latest log `Saved/Logs/v0.5.4-final-after-escape-game-build.out.log`.

### Full ProjectRift automation

Report: `Saved/Automation/Reports/v0.5.4-final-after-escape-fix`  
Result: 53/53 succeeded, 0 failed. Fourteen tests emitted 47 expected warning events from deliberate rejection/failure paths and existing diagnostics.

## ProjectA MCP and PIE verification

- Endpoint used: only `http://127.0.0.1:8001/mcp`.
- MCP created, saved, and read back `/Game/ProjectRift/ShipRepairs/DA_RepairEngineStage1` with engine level 1, `EnergyCrystal ×10`, the Prologue story prerequisite, and Chapter One results.
- Two-player listen-server PIE created two bound PlayerStates with distinct profile GUIDs and no pending repair save.
- At a 966x580 floating client window the R panel opened centered without covering the lower-left diagnostics. Escape focus is protected by the new regression test; final OS-level key injection was intentionally stopped after Windows focus restrictions made it unreliable.

## Manual acceptance matrix

- [ ] Create a clean profile; confirm the R panel reports missing story and resources.
- [ ] Use `[开发验收] 准备档案`; confirm it adds only the required story node and raises `EnergyCrystal` to 10.
- [ ] Execute the repair; confirm resources become 0, engine becomes level 1, and `Chapter.One` becomes unlocked/current.
- [ ] Repeat the action, close/reopen the panel, and restart; confirm no second deduction.
- [ ] Use a second profile; confirm resources, modules, chapters, and transaction history remain isolated.
- [ ] Bind two different multiplayer profiles and repair only one; confirm the other player is unchanged.
- [ ] Simulate a save failure; confirm readying and another repair are blocked until the five-second retry succeeds.
- [ ] Travel lobby-to-rift-to-lobby; confirm resources, ship modules, and chapter state survive.

## Suggested Git metadata

Suggested commit message: `feat: add ship repair and chapter unlock progression`  
Suggested tag after acceptance and user commit: `v0.5.4`
