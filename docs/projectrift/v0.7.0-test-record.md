# ProjectRift v0.7.0 Test Record

Date: 2026-07-19  
Project version: `0.7.0`  
Profile save format: `v5`

## Focused automation

- `ProjectRift.Items.InstanceIdentity`: deterministic v4-to-v5 migration, profile namespace separation, and duplicate current GUID rejection.
- `ProjectRift.Items.InstanceIdentity.Runtime`: new stacks mint identities and merge targets retain identity.
- `ProjectRift.Items.AuthoritativeTransactions.Contract`: transaction component, revision, RPC, and replay-cache contract coverage.
- `ProjectRift.Items.AuthoritativeTransactions.Runtime`: pending reload, replay, revision conflict, GUID-targeted consumption, and authoritative reload completion.
- `ProjectRift.Items.AuthoritativeTransactions.Pickup`: server GUID pickup transfer and post-commit destruction.
- `ProjectRift.Items.AuthoritativeTransactions.Drop`: destination validation, split identity, and transactional inventory delta.
- `ProjectRift.Save`: profile migration/storage/runtime bridge regression coverage.
- `ProjectRift.Items.ConsumableUse` and `ProjectRift.Weapons.Runtime`: existing player-facing mutation regressions.

## Final verification

- Direct `ProjectRift` automation: 59 passed, 0 failed, 0 not run.
- `Invoke-ProjectRiftLocal.ps1 -Mode Quick`: succeeded; `ProjectRift.Smoke` 3 passed, 0 failed.
- `Invoke-ProjectRiftLocal.ps1 -Mode Full`: succeeded; 85 passed, 0 failed; Development BuildCookRun and `ProjectRift.LocalSmoke` both passed.
- `Invoke-ProjectRiftLocal.ps1 -Mode Build -Target Package -Configuration Shipping`: succeeded; Shipping BuildCookRun and bounded NullRHI launch probe passed.
- Shared `UnrealEditor.modules` remained `BBE466E4330F3FDDEA1120A9070A7402F5A9655404223F55C9E0D1D322568747`, length `32290`, throughout the Quick, Full, and Shipping gates.

## Manual acceptance still required

1. In two-player PIE, verify GUID-targeted pickup, partial/full drop identity behavior, consumable use, and stale revision/retry behavior.
2. Verify reload cancellation on stun/down/death/pawn loss preserves reserve ammo, and successful completion uses the current reserve amount.
3. Verify reconnect/seamless PlayerState replacement preserves the session-only transaction state and v5 profile identity integrity.
