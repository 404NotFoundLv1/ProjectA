# ProjectRift v0.8.0 Test Record

## Implemented scope

- Added data-driven mission contract fields to the existing mission progression asset, deterministic runtime definitions, reward previews, and compact `ContractId`/version/seed travel context.
- Added host-authoritative selection, replicated definition state, readiness reset on selection, rift-side version validation, and settlement contract-version recording without changing the profile save format.
- Added the native text-first mission selection fallback and its PlayerController lifecycle. It reads the AssetManager catalog, presents locked entries, and sends only the selected contract ID; the server supplies the seed and accepts or rejects the request.

## Automated verification

- `ProjectRift.MissionContracts`: 3/3 passed (definition determinism, malformed-input rejection, compact context round-trip, legacy `MissionId` parsing, and native selection-widget contract).
- `ProjectRift.Rift`: 7/7 passed after the lobby/rift integration.
- `ProjectRift.Smoke`: 3/3 passed.
- Quick regression: Editor build and `ProjectRift.Smoke` 3/3 passed.
- Full `ProjectRift` automation: 103/103 passed; the Development package and `ProjectRift.LocalSmoke` NullRHI run also passed.
- Shipping `BuildCookRun` and bounded Shipping NullRHI startup check both passed.
- The shared `UnrealEditor.modules` manifest remained unchanged: SHA-256 `BBE466E4330F3FDDEA1120A9070A7402F5A9655404223F55C9E0D1D322568747`, length `32290`.

## Manual acceptance checklist

- In two-player PIE, confirm that only the host can select a mission; a locked chapter is rejected without changing the current selection.
- Confirm both clients show the same biome, difficulty, modifiers, reward preview, contract version, and server-generated seed; selection clears readiness.
- Start the rift and return to the lobby; confirm settlement is attributed to the selected contract and the next selection succeeds.
- If production mission-table presentation is desired, create the three authored mission assets and the optional widget layout through ProjectA's `127.0.0.1:8001/mcp`; no maps or existing Blueprints were saved by this implementation.
