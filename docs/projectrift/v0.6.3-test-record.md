# ProjectRift v0.6.3 test record

## Scope

Data-driven roles and the finalized Assault module framework. Save format remains v4.

## Focused evidence

- `ProjectRift.Roles.DataAssets` validates the role/module primary asset catalog.
- `ProjectRift.Roles.Loadout` validates replicated Q/E/R loadouts and authoritative replacement.
- `ProjectRift.Roles.Loadout.UI` validates the native panel and reliable controller RPC contract.
- `ProjectRift.Roles.Persistence` validates v4 lossless round-tripping of known and unknown role-module state.

## Manual acceptance still required

- In a two-player 8001 PIE ship lobby, press `C`, restore defaults, apply, and verify only waiting players may alter the Q/E/R loadout.
- Verify the lobby line and GAS debug panel report the role and equipped Q/E/R module IDs.
- Verify the v4 profile preserves total role/module unlock state across a save/reload, including unknown legacy IDs.
