# ProjectRift v0.5.3 test record

Date: 2026-07-13  
Branch: `main`  
Base commit: `5361c7a`

## Implemented scope

- Registered `UPRMissionProgressionDataAsset` as `ProjectRiftMission` and created `/Game/ProjectRift/Missions/DA_RiftTestMissionProgression` for `Mission.Rift.Test.Hold` and `L_Rift_Test`.
- Added host-led mission selection/eligibility, MissionId ServerTravel validation, and replicated lobby mission/can-start state.
- Added strict profile projection validation, reliable binding RPCs, duplicate GUID rejection, session profile locking, and seamless travel preservation of runtime state and baselines.
- Added authoritative per-player settlement receipts, extracted/non-extracted/failure inventory and resource policies, local transactional persistence, idempotent receipt handling, acknowledgements, retry state, and the four-to-eight-second return barrier.
- Upgraded player saves to v3 with a 128-entry processed settlement ledger and chained v1-to-v2-to-v3 migration.
- Added development-only lobby/settlement diagnostics and isolated multi-client PIE profile roots below ProjectA `Saved/Automation`.

No Steam Cloud, account authentication, anti-cheat, disconnect compensation, host-managed remote saves, formal mission selection UI, or later-version feature was implemented.

## TDD evidence

- RED builds first failed on the missing mission/projection contracts and v3 settlement ledger, then passed after those contracts were implemented.
- `ProjectRift.MultiplayerProfile.Contract` failed on seamless PlayerState inventory loss; the travel copy path was fixed to preserve absolute inventory and the test passed.
- Projection validation first accepted duplicate story nodes; strict bounded story/name/payload validation was added and the test passed.
- The non-extracted policy test first failed on the missing resource-wallet policy; the implementation now caps the wallet to the mission baseline while preserving actual consumption.
- Settlement write refusal is covered by a deterministic future-version write barrier: the old in-memory snapshot remains unchanged and the receipt stays pending for lobby retry.

## Automated verification

### Builds

- `ProjectAEditor Win64 Development`: succeeded.
- `ProjectA Win64 Development`: succeeded.

### Full ProjectRift automation

Report: `Saved/Automation/Reports/v0.5.3-final`  
Result: 47/47 leaf tests passed; 36 clean successes, 11 successes with expected test warnings, 0 failed, 0 not run.

Coverage includes mission contracts, host eligibility policy, profile/schema migration, safe storage, session locking, projection/RPC contracts, settlement idempotency and write failure, inventory/resource retention, lobby/rift flow, settlement display, AssetManager registration, and all pre-existing ProjectRift regressions.

## ProjectA MCP verification

- Endpoint used: only `http://127.0.0.1:8001/mcp`.
- Mission asset properties were written, saved, and read back through MCP.
- Two-player listen-server PIE used separate roots:
  - `Saved/Automation/PIEProfiles/v0.5.3/PIE_0`
  - `Saved/Automation/PIEProfiles/v0.5.3/PIE_1`
- Server-world inspection showed two bound PlayerStates with distinct valid profile GUIDs and the replicated `Mission.Rift.Test.Hold` team mission.
- Main-menu/lobby capture showed the v0.5.3 profile panel, GAS panel, and lobby team panel in separate regions. The lobby panel reported `Contract: VALID`, `Can start: NO`, and `Waiting for all players to ready` before readiness.

## Manual acceptance matrix

- [ ] Bind different host/client profiles; confirm both must bind and ready before start.
- [ ] Try the same GUID; confirm only the later player is rejected and the first remains bound.
- [ ] Give host/client different story progress; confirm only host eligibility controls mission selection/start.
- [ ] Extract only one eligible player; confirm only that player advances story and retains extracted results.
- [ ] Confirm a non-extracted player loses new loot/resources but keeps actual item/resource consumption and role selection.
- [ ] Fail a run; confirm existing failed resource retention remains 50% rounded down and story does not advance.
- [ ] Reapply the same receipt before and after restart; confirm resources, inventory, and story change only once.
- [ ] Simulate one save failure or missing response; confirm return occurs within eight seconds and lobby retry status appears.
- [ ] Return to lobby and restart both profiles; confirm their data remains isolated.

## Suggested Git metadata

Suggested commit message: `feat: add multiplayer profile progression and host-led story policy`  
Suggested tag after acceptance and user commit: `v0.5.3`
