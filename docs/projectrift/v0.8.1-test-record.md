# ProjectRift v0.8.1 Test Record

## Scope

- Objective graph and five primary objective contracts.
- Profile format remains v9; legacy single-objective missions remain supported.

## Automated evidence

- `ProjectRift.Rift.ObjectiveGraph`: 6 passed, 0 failed, including the five-type serial/parallel and snapshot-recovery flow (`20260721T113910Z-8946d25d-cceb-4d79-9d7c-9643c962871e`).
- `ProjectRift.Rift.GameModeState`: 1 passed, 0 failed after objective-map binding (`20260721T112449Z-4f7b819e-ae11-409b-b00e-6de17ea9fe17`).
- `ProjectRift`: 109 passed, 0 failed in the final regression (`20260721T114315Z-82763cab-68d6-42ff-adb9-674d40e94233`).
- Editor build: passed in the same local pipeline.
- Development BuildCookRun and `ProjectRift.LocalSmoke`: passed in the final regression.
- Shipping BuildCookRun, test-module exclusion, and shipping launch probe: passed (`20260721T112921Z-7fc863d0-41f4-431f-9a13-ee548bc1b12d`).
- Shared `UnrealEditor.modules` hash remained `BBE466E4330F3FDDEA1120A9070A7402F5A9655404223F55C9E0D1D322568747` (32290 bytes).

## Authored ProjectA assets

- Mission: `/Game/ProjectRift/Missions/DA_RiftObjectiveGraphMission` (`Mission.Rift.Test.ObjectiveGraph`).
- Quest items: `DA_ObjectiveSample` and `DA_ObjectiveCore`.
- Objective Blueprints: `BP_ObjectiveGraphHold`, `BP_ObjectiveCollect`, `BP_ObjectiveCarry`, `BP_ObjectiveDestroy`, and `BP_ObjectiveHunt`.
- Map-specific GameMode: `BP_ObjectiveGraphRiftGameMode`, whose default `MissionId` is `Mission.Rift.Test.ObjectiveGraph`; `L_Rift_ObjectiveGraph_Test` explicitly references this class so direct PIE enters the graph mission instead of the ship-lobby GameMode.
- Test map: `/Game/ProjectRift/Maps/L_Rift_ObjectiveGraph_Test`, duplicated from `L_Rift_Test` and saved separately.
- The authored graph is `Collect -> Carry -> Hunt` in parallel with `Collect -> Destroy -> Hold`; all five nodes are required. The three sample pickups and the core have distinct GUIDs, and the core is gated until Carry activates.
- Readback confirmed the objective graph, Hold node/15-second override, Core GUID/node binding, and both original/duplicate map packages were clean after save.
- Direct ProjectA PIE on `L_Rift_ObjectiveGraph_Test` started `BP_ObjectiveGraphRiftGameMode` and logged `MissionId=Mission.Rift.Test.ObjectiveGraph` with a successful Rift mission start.

## Remaining manual acceptance

- Run two-player PIE on `L_Rift_ObjectiveGraph_Test`; activate Collect, collect three samples, complete Carry and Hunt, destroy the anchor, then hold for 15 seconds.
- Verify shared tracker summaries for both clients, enemy Hunt attribution, Destroy damage through GAS, disconnect/reconnect state recovery, and core respawn after a dropped core is absent for ten seconds.
