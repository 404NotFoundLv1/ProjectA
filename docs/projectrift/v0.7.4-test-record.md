# ProjectRift v0.7.4 Test Record

Project version: `0.7.4`  
Profile schema: `v8`

## Automated coverage

- `ProjectRift.Items.QuickbarContract`: reflected quickbar, PlayerState, item-use metadata, HUD, and v8 profile contract.
- `ProjectRift.Items.ConsumableUse`: verifies the legacy inventory command now starts an unbound timed use, retains the source stack until completion, and applies its configured GameplayEffect only at completion.
- Existing save migration, item identity, reward persistence, gameplay contract, diagnostics, and smoke version checks were updated to v8 / v0.7.4.

## Manual acceptance checklist

- Bind four consumables to slots 1--4 and confirm the HUD shows the assigned GUID-backed items after save/load and PlayerState replacement.
- Use health, shield, energy, purifier, and ammunition packs at partial resources; verify use time, cooldown, atomic consumption, and no consumption on cancellation.
- Confirm direct physical damage, stun, hit stagger, downed state, and pawn removal cancel an active use; pollution ticks must not.
- Verify task-tool-gated objectives reject players without their declared tool and consume it through the authoritative transaction when configured.
- Confirm a data-marked auto-process task item is absent from the personal settlement snapshot and its quickbar reference is cleared.

## Verification evidence

- Targeted quickbar, consumable-use, profile-schema, and asset-manager contracts passed; the latter confirms all 13 ProjectRift item assets and their v0.7.4 use definitions.
- `Quick` passed: Development editor build plus `ProjectRift.Smoke` (3/3).
- Final `Full` passed: 96/96 ProjectRift automation tests, Development package, and `ProjectRift.LocalSmoke` (`PASS`).
- Shipping BuildCookRun passed, including test-module exclusion and the NullRHI startup probe.
- A packaging-only startup fault was found during verification: file-scope dynamic `FGameplayTag` creation in the new quickbar component could occur before the packaged game's tag manager was ready. Tags now resolve lazily on first gameplay use; both Development LocalSmoke and Shipping startup then passed.
- Shared `UnrealEditor.modules` remained unchanged: SHA-256 `BBE466E4330F3FDDEA1120A9070A7402F5A9655404223F55C9E0D1D322568747`, length `32290`.
